> 本文档迁移自 Icstick/acp-docs（commit 34e2840），2026-09-08 并入本仓库 docs/design/。
> 内容截至 2026-09-03；与当前实现不一致处以本仓库 README.md / AGENTS.md / src 为准（acp-docs 仓库已归档）。
# Case G — 写后立即读取

**日期**：2026-08-26
**固定条件**：
```text
DSH commit:        2c2da5acf555d2828ae511292f704f79cffc72be + fix/benchmark-fg（CJK recall 修复）
LLM:               host（未调用）
embedding:         MemOS local（all-MiniLM-L6-v2 q8）
context budget:    900 tokens
temperature:       0
MemOS viewer:      127.0.0.1:18801
```

## 过程
写入显式纠正（correct："用户喜欢详细的技术回答"，无 targetId 孤儿纠正）→ 立即 recall（"喜欢什么风格回答"）→ 检查纠正内容是否可命中。
每 run 独立 sourceRef（bench-g-{0,1,2}）。

## 指标（3 runs）
| 指标 | run1 | run2 | run3 |
|---|---|---|---|
| correctionInserted | true | true | true |
| immediatelyVisible | true | true | true |
| latencyMs | 7 | 1 | 0 |

## 结论
**通过**。显式纠正写后立即 recall 可命中（CJK 短查询场景）。

## 修复记录（2026-08-26）
首跑失败：recall 用整串 LIKE（contentSubstr），"喜欢什么风格回答" 无法命中 "用户喜欢详细的技术回答"
（CJK 无分词）→ 立即可见性 false。已修复：
1. store.query 增加 contentAnySubstr（OR 语义）
2. service.recall 用 recallSubstrings()：整串 + CJK bigram（连续 2 字符窗口）OR 召回
3. composer.lexicalScore 同步增强 CJK bigram 重叠（0.3 兜底 → 精确比例）
4. 新增回归测试（test/user-rights.test.mjs + composer.test.mjs 更新）

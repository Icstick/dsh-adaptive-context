> 本文档迁移自 Icstick/acp-docs（commit 34e2840），2026-09-08 并入本仓库 docs/design/。
> 内容截至 2026-09-03；与当前实现不一致处以本仓库 README.md / AGENTS.md / src 为准（acp-docs 仓库已归档）。
# Case B — 偏好及明确纠正

**日期**：2026-08-26
**固定条件**：
```text
DSH commit:        2c2da5acf555d2828ae511292f704f79cffc72be（master，benchmark 时分支 fix/benchmark-fg）
LLM:               host（未调用）
embedding:         MemOS local（all-MiniLM-L6-v2 q8）
context budget:    900 tokens
temperature:       0
MemOS viewer:      127.0.0.1:18801
```

## 过程
写入旧偏好（"默认用 pnpm"）→ correct() 修正（"更正：之后统一用 Bun"）→ 查询当前真相（"用什么包管理器"）。
每 run 独立 sourceRef（bench-b1-{n} / bench-b2-{n}）。

## 指标（3 runs）
| 指标 | run1 | run2 | run3 |
|---|---|---|---|
| corrected（inserted && superseded） | true | true | true |
| oldState | superseded | superseded | superseded |
| currentTruth（recall 含 Bun） | false | false | false |
| latencyMs | 7 | 1 | 1 |

> currentTruth=false 需说明：recall 查询 "用什么包管理器" 的整串/bigram 与 "更正：之后统一用 Bun" 无重叠（'用什么'/'包管理' 等窗口 vs 内容），是 MVP 检索召回的已知限制——与 correct 语义无关（supersede 状态迁移正确）。真实场景由 Context Composer 的 query 侧召回（含 MemOS semantic 层）覆盖。

## 结论
**通过**（纠正语义）。新偏好覆盖 current truth 的状态机正确：旧证据 superseded、保留历史、可审计；latency 毫秒级。

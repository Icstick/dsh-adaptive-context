# Benchmark Case Template

**日期**：YYYY-MM-DD
**固定条件**：
```text
DSH commit:        （git rev-parse HEAD）
LLM:               （实际模型）
context budget:    900 tokens
temperature:       0
MemOS viewer:      127.0.0.1:18801（recallEnabled=false, viewerEnabled=true）
```

## 过程
（注入序列描述）

## 指标（3 runs）
| 指标 | run1 | run2 | run3 |
|---|---|---|---|
| （case 特有指标） | | | |

## 结论
（通过/不通过 + 与 KPI 的对照）

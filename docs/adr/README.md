# ADR 索引（dsh-adaptive-context）

架构决策记录。规则（轻量版）：
1. **何时写**：引入跨模块约束/存储选型/状态模型/边界划分的决定；日常实现细节不写。
2. **格式**：状态（采纳/废弃）→ 背景（≤5 行）→ 决策（一句话可执行）→ 后果（已知代价）。
3. **只追加**：不修改历史 ADR；决定变化写新 ADR 并在新条目注明 supersedes NNN。
4. 详情（长文）见 acp-docs 仓库对应文档；ADR 是索引与契约锚点。

| 编号 | 决策 | 状态 |
|---|---|---|
| 0001 | 证据账本用自建 append-only SQLite（node:sqlite 单连接），视图可重建 | 采纳 |
| 0002 | authority 模型：7 值确定性声明 + sourceClass 强制映射 + 审计三链 | 采纳 |
| 0003 | 撤销语义 supersede 方案甲：新证据存被替代证据 id（直接前驱）+ 两级冲突检测 | 采纳 |

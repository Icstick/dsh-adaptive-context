> 本文档迁移自 Icstick/acp-docs（commit 34e2840），2026-09-08 并入本仓库 docs/history/（开发史保档）。
> 内容截至 2026-09-03，仅作追溯；当前状态以 docs/DEVELOPMENT-PLAN.md 与 docs/PLAN-S2-MIGRATION.md 为准（acp-docs 仓库已归档）。
# HANDOFF — 2026-08-26 收工交接（明日续接）

> 目的：压缩今天的工作上下文，明日无缝续接。
> 相关：docs/DEVELOPMENT-PLAN.md（进度）、docs/BENCHMARK.md（验收）、docs/benchmark-results/（结果）。

## 当前状态（2026-08-26 晚）

- **M1 完成判定达成**：代码 + benchmark + 集成实测全绿，master @`8a7bddd`
- 81/81 测试通过（core 76 + work 5）
- benchmark A/B/F/G 通过并归档（其中 F/G 含缺陷修复记录）

## 今日成果

1. **MemOS 修复**（正式环境）
   - embedder 模型缓存下载失败 → hf-mirror 手动下载 q8 模型 → search 10.7s → 131ms
   - 模型稳定库：`D:\DSH_workspace\acp-assets\minilm`（pnpm install 会清 node_modules 里的
     transformers/.cache，需重新复制到两个 profile 的 .cache/Xenova/）
2. **benchmark A/B/F/G**：通过 + F（注入模式扩展）/G（CJK bigram 召回）缺陷修复
3. **Config schema 修复**（用户修复，我审查提交 @0b5216e）：插件 Config 改 Schemastery schema
4. **dev 环境建设**（独立验证环境，可自主重启）：
   - 副本：`D:\DSH_workspace\dsh-dev-harness`（源码 217MB；node_modules 走 pnpm store 硬链接）
   - dev home：`D:\DSH_workspace\.dsh-dev`（sessions/ledger/memos 全隔离；凭据/settings/SOUL 已复制）
   - dev profile：ACP + work-continuity + MemOS，端口 **3081 web / 18802 memos viewer**
   - 重启：`D:\DSH_workspace\dsh-dev-restart.bat`（先杀端口进程再启动，日志 dsh-dev-web.log）
   - 手动启动：`$env:DSH_HOME='D:\DSH_workspace\.dsh-dev'; cd D:\DSH_workspace\dsh-dev-harness; node --import tsx/esm apps/cli/src/bin.ts --profile dev --no-open`
5. **DSH seam 契约校准（3 个真实契约 bug 修复 @8a7bddd）**：
   - agent/pre-step 必须返回 PreStepDecision（原返回 undefined → DSH 读 undefined.kind 崩 turn）
   - session/event 签名是 (session, event)（原把 session 当 event 解析）
   - 真实事件类型是 agent/inbox/spliced（data.inserted[]）而非 user/message；sourceRef 用 sessionId:seq
6. **端到端验证通过**：S1 写事实 → 摄入 ledger → S2 新会话注入 → 模型答 "pnpm"

## 关键经验（详见记忆 lesson）

- DSH 插件 seam 契约以真实源码为准：hooks-codex（pre-step 范式）+ memos DSH adapter（注入范式）
- headless 驱动：POST /api/<method> + {type:'client-request', rpcId, method, payload}；
  session.create / session.prompt / session.history / session.list / session.models；
  turn 状态看 events 里 turn/end 的 reason
- dsh CLI：`dsh web` 硬编码 web profile；自定义 profile 用 `dsh --profile <name>`
- pnpm-workspace.yaml allowBuilds 是 `包名: bool` 映射，不是数组
- 审批策略今日起为 never + danger-full-access（不再弹审批）

## 明日待办

1. **正式 web profile 挂 ACP**（待用户拍板）：package.json 加两行 link + bundles →
   pnpm install（需先停正式 DSH 释放 better-sqlite3 锁）→ 重启 → GUI 会话实测注入
2. /checkpoint 命令实测（dev 环境）
3. M2 v0.1 规划：background consolidation / temporal truth / Expression manual promotion
4. memos-provider 接入 composer 的 hasProvider=true 路径（当前 hasProvider=false）
5. research/ 目录清理（Hindsight.zip 169MB）

## 环境速查

| 实例 | Web | MemOS viewer | Home | 树 |
|---|---|---|---|---|
| 正式（GUI） | 3080 | 18801 | C:\Users\Administrator\.dsh | D:\deepseek-harness |
| dev | 3081 | 18802 | D:\DSH_workspace\.dsh-dev | D:\DSH_workspace\dsh-dev-harness |

- dev 实例当前可能还在运行（pwsh 后台 job）；重启用 dsh-dev-restart.bat
- ACP ledger：正式 `C:\Users\Administrator\.dsh\acp\acp-ledger.db`（仅 benchmark 数据）、
  dev `D:\DSH_workspace\.dsh-dev\acp\acp-ledger.db`（集成测试数据）

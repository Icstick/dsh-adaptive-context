> 本文档迁移自 Icstick/acp-docs（commit 34e2840），2026-09-08 并入本仓库 docs/history/（开发史保档）。
> 内容截至 2026-09-03，仅作追溯；当前状态以 docs/DEVELOPMENT-PLAN.md 与 docs/PLAN-S2-MIGRATION.md 为准（acp-docs 仓库已归档）。
# HANDOFF — 2026-08-27 收工交接（明日续接）

> 目的：压缩今天的工作上下文，明日无缝续接。
> 相关：DEVELOPMENT-PLAN.md（进度）、M2-PLAN.md（M2 决策与拆解）、LESSONS.md（经验汇总）、CODE-MAP.md（模块地图）。

## 当前状态（2026-08-27 晚）

- **M1 全绿 + M2 v0.1 六任务合入并 dev 验证通过**
- dsh-adaptive-context master @ 8c854ce，**119/119 测试**
- acp-docs 独立 git 库 @ bda1d04（M2-PLAN/CODE-MAP/DEVELOPMENT-PLAN/LESSONS 已同步）

## M2 成果（六任务）

| 任务 | 内容 | 验证 |
|---|---|---|
| T1+T2 | self-echo 过滤 + contentHash dedup | dev 实测注入无当前消息 ✓ |
| T3 | MemOS provider 语义分接入 | 集成测试 ✓ |
| T4 | background consolidation（队列/节流/LLM 派生/规则兜底/observation 表 v2） | dev 实测 5 条 observation（LLM 提取精准）✓ |
| T5 | temporal 双视图（allowSuperseded + lineage） | 测试 ✓ |
| T6 | expression approval 审批门（requestPromotion 桥） | 测试 ✓（style 候选真实触发待观察） |

执行方式：5 git worktree + flash 子代理并行 → --no-ff 合流（3 处冲突手工解决）→ 119/0 回归 → dev 验证。
集成缺陷修复：llm.stream purpose 非法值 → compaction（e939343）。

## 明日待办

1. **worktree 清理**：wt-a~wt-e 已全部合入，git worktree remove 各目录 + 删分支
2. **正式实例配置**：下次重启正式 DSH 会自动加载 M2（junction 同目录）。重启前在
   web profile 的 cordis.patch.yml 给 adaptive-context 配：

       consolidationProvider: deepseek-official
       consolidationModel: deepseek-v4-flash

   （不配则 consolidation 走规则兜底；provider 名按正式 settings 可用模型组定）
3. **style 审批门真实验证**：等 consolidation 产出 style 域候选时观察 approval 请求
4. **M3 v1 规划**（可选启动）：provider routing / guarded auto promotion / audit/export/rebuild

## 环境速查

| 实例 | Web | MemOS | Home | 状态 |
|---|---|---|---|---|
| 正式 | 3080 | 18801 | C:\Users\Administrator\.dsh | 运行中（M1 代码，重启后 M2） |
| dev | 3081 | 18802 | D:\DSH_workspace\.dsh-dev | 运行中（M2 代码，consolidation 已配 LLM） |

- dev 重启（bat 有 Input redirection 问题，pwsh 直接跑）：
  $env:DSH_HOME='D:\DSH_workspace\.dsh-dev'; cd D:\DSH_workspace\dsh-dev-harness; node --import tsx/esm apps/cli/src/bin.ts --profile dev --no-open
- dev 杀旧实例：Get-NetTCPConnection 3081/18802 → taskkill /F /T
- 测试：cd D:\DSH_workspace\my-plugins\dsh-adaptive-context; node test\&lt;file&gt;.test.mjs

## 今日关键注意点（详见 LESSONS.md）

- GenerateOptions.purpose 联合类型（compaction|session-title），自定义值静默失败
- worktree + junction + flash 并行模式跑通（workflow 600s 墙钟，大项转 subagent 后台）
- 合并时 self-echo 规则与短 query 测试冲突（测试 query 写真实长度）
- 正式实例 junction 同目录 = 重启即升级（升级前先配 consolidation LLM 路由）

---

## 追加（2026-08-27 晚，本轮进展）

- **style 审批门契约修正**（b5981dc，122/0）：approval 需 agent + outcome allowed-once；
  consolidation 后台标 pending_promotion → pre-step fire-and-forget 发审批。
  dev 实测全链路通过：pending 标记 → approval/asked 事件 → turn 不阻塞。
  **待用户 dev UI（3081）响应审批**：允许 → reviewStatus=promoted；拒绝 → dismissed。
- **正式实例已升级 M2**（用户意外重启主 agent）：consolidation 配置生效。
- **修复 patch 同 id 覆盖**：ledgerDir 与 consolidation 合并为一块（下次重启数据归位 .dsh\acp）。
- **数据分散现状**：.dsh\acp 47 条 + deepseek-harness\acp 20 条（v0.1 迁移合并）。
- **LLM style 分类噪声**：无关历史消息被带进 style 候选（v0.1 优化 prompt 过滤）。

### 最新待办

1. dev UI 响应审批 → 确认 reviewStatus 迁移（当前挂起 2 个 acp.promotion 请求）
2. 正式实例下次重启（patch 已合并）→ ledger 归位验证
3. 数据迁移合并脚本（v0.1）
4. M3 v1 规划

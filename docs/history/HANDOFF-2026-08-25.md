> 本文档迁移自 Icstick/acp-docs（commit 34e2840），2026-09-08 并入本仓库 docs/history/（开发史保档）。
> 内容截至 2026-09-03，仅作追溯；当前状态以 docs/DEVELOPMENT-PLAN.md 与 docs/PLAN-S2-MIGRATION.md 为准（acp-docs 仓库已归档）。
# HANDOFF — 2026-08-25 收工交接（明日续接）

> 目的：压缩今天的工作上下文，明日（重启 DSH 后）无缝续接。
> 相关：docs/DEVELOPMENT-PLAN.md（任务清单）、docs/CODE-MAP.md（模块地图）、docs/BENCHMARK.md（验收）。

## 当前状态（2026-08-25 晚）

- **M1 MVP 代码全部完成**：74/74 测试通过，master 最新 `40577cb`
- 五个模块全合回 master：evidence-ledger / context-composer / governance / work-continuity / memos-provider（代码）
- **唯一未完成：真实 benchmark A/B/F/G**——需要 DSH 重启 + 运行环境

## 已完成模块（master）

| 模块 | 位置 | 测试 |
|---|---|---|
| Evidence Ledger | packages/dsh-adaptive-context/src/{store,extract,lifecycle,governance,service,index}.mjs | 10+6+9+15+7+9=56（conformance 含） |
| Context Composer | src/{budget,composer}.mjs | 8 |
| Governance 用户权利 | src/service.mjs（correct/release/redact/delete 等） | 7 |
| Work Continuity | packages/dsh-work-continuity/（store + /checkpoint） | 5 |
| MemOS Provider | src/providers/{recall-contract,memos}.mjs | 5 |
| benchmark 脚本 | scripts/benchmark-case.mjs | — |

## MemOS 安装状态（web profile）

- **已装入**：@memtensor/memos-local-plugin@2.0.16（npmmirror 解决 onnxruntime 下载）
- **已配置**（~/.dsh/profiles/web/）：
  - package.json：dependencies + dsh.profile.bundles 均已加
  - pnpm-workspace.yaml：allowBuilds 已补（better-sqlite3/esbuild/onnxruntime-node/sharp）
  - cordis.patch.yml：覆盖 memos-local-memory（recallEnabled=false、toolsEnabled=false、viewerEnabled=true、captureEnabled=true）
- **未重启 DSH** → 尚未生效

## 明日验证步骤（重启后）

```bash
# 1. 重启 DSH（web profile）
# 2. 验证 MemOS viewer 生效：
#    curl http://127.0.0.1:18801/api/v1/health  或浏览器开 http://127.0.0.1:18801
#    预期：200 + viewer 页面（若 18801 被占，检查 cordis.patch.yml viewerPort）
# 3. 验证插件装载：
#    dsh --profile web --dump-config | grep memos-local-memory
# 4. 跑 benchmark（每 case 3 runs）：
cd D:\DSH_workspace\dsh-adaptive-context\packages\dsh-adaptive-context
node scripts/benchmark-case.mjs A
node scripts/benchmark-case.mjs B
node scripts/benchmark-case.mjs F
node scripts/benchmark-case.mjs G
# 5. 结果写入 docs/benchmark-results/case-<X>-<date>.md（用 template.md）
# 6. 对照 KPI 判定通过/不通过（BENCHMARK.md §2）
```

## 下一步计划

1. 真实 benchmark A/B/F/G（明日）→ 结果归档 → M1 正式完成
2. 集成验证：ACP 插件挂进 web profile（link + bundles + 重启）——**还没做**！
   （当前只有 MemOS 进了 profile，ACP 自己的插件还没挂载）
3. 挂载后实测：agent/pre-step 注入、Evidence 摄入、/checkpoint 命令
4. M2 v0.1 规划：background consolidation / temporal truth / Expression manual promotion

## 关键提醒

- **ACP 插件本身未挂进 profile**——明天验证完 MemOS 后，需要把 dsh-adaptive-context 和
  dsh-work-continuity 也 link 进 web profile（参考 dsh-memento 的挂载方式：
  dependencies link + bundles + pnpm install + 重启）
- MemOS 的 `profileId` 默认 'default'，session 有 agentPreset 时会覆盖——注意命名空间
- benchmark 脚本假设 MemOS viewer 在 127.0.0.1:18801（createMemosProvider 默认值）
- 环境坑（已记记忆）：npm 大包用 npmmirror；构建脚本需 danger-full-access；git 管道命令被拦

## 待清理

- research/ 目录：MemOS/Hindsight/personagent 解压源码（zip 保留，可删解压目录省空间）
- D:\DSH_workspace\research\*.zip：Hindsight 169MB 最大，可考虑归档

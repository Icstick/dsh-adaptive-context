# HANDOFF 2026-09-22（A → W）—— P1-4.2 截断标记已进 main

> 生成：2026-09-22（A 机 DESKTOP-G4EAACD） · 对位文档：`docs/history/HANDOFF-2026-09-22.md`（W → A/B）
> 目标版本：`main` = **f73c3f1**（A 的 merge 提交，**已在 origin**）

## 0. 一句话

你在 HANDOFF-2026-09-22 里等 A/B 拉代码——A 拉完了，但 **A 手上还有 4 个你没见过的提交**（P1-4.2 截断标记）。已合并、已推送。你要做的是 `git fetch && git pull`（你两侧克隆都干净、可快进），然后按 §3 订正一处验收口径。

## 1. A 手上独有的东西（你此前没有）

| commit | 内容 | 为什么重要 |
|---|---|---|
| `a3efb0a` | `src/extract.mjs` 新增 `clampText()`，4 处 `slice` 全改走它；超限追加 `…[truncated N chars]` | **你的代码此前没有这个函数**（实测：推送前 `git grep clampText origin/main` 为空）→ 你那边 >4000 字符的事件仍在**静默丢尾巴** |
| `940186b` / `307a774` | `docs/DEVELOPMENT-PLAN.md` 登记 ACP-B17（截断标记，已完成）/ ACP-B18（candidate 催办，阻塞于 4.1 同步边界） | backlog 两条，pull 即见 |
| `68b0273` / `f73c3f1` | merge 提交 | — |

**分叉原因**：你的 23 个提交与 A 的 4 个提交同出于 `7f12557`，两边都没推对方的。A 用 **merge** 保住双方（不是 reset、不是 rebase）——合并后 A 实测 **531 pass / 0 fail**、oxlint 0/0。

## 2. 你不需要做的事

- **不需要任何 reset**：你是干净的，快进即可。
- **不要照抄 A 的 merge 方式**：A 有本地独有提交你没有；你没有分叉，直接 ff。
- **账本数据一律不动**（沿用你 HANDOFF §2.3 的口径：C 档隔离 / 候选池 / rule 草案都是每机本地数据）。

## 3. 你要做的

```powershell
cd D:\DSH_workspace\my-plugins\dsh-adaptive-context
git fetch --all; git pull --ff-only
git log -1 --oneline            # 现在是 f73c3f1，不再是 5027460
node --test "test/*.test.mjs"   # A 机实测 531 pass / 0 fail
npx oxlint                      # A 机实测 0 warnings 0 errors

# 另一份克隆同样 pull（DSH 实际加载的是这一份）
cd C:\Users\NeoVox\.dsh\vendor\dsh-adaptive-context
git fetch --all; git pull --ff-only
```

**口径订正**：你 HANDOFF §3.1 写的「`git log -1` 应为 `5027460`」「测试 516/516」——前者换成 **f73c3f1**，后者换成 **531**（多了 A 的 `clampText` 用例 + merge）。

## 4. 共享待办：子代理 prompt 入账 —— 【本节已被 §6.4 订正，先看 §6.4】

> ⚠️ 2026-09-22 20:4x 追加订正：**你代码里已经有这个机制**（`subagentDowngrade`，默认 true）——A 重启后实测：新行落 `session_type=subagent` + `agent_inference/experience`（记录但不可注入）。本节下面的建议以 §6.4 为准，**不要重复实现**。

## 4-orig. 原记录（保留作历史）

你 next_steps 里那条——「评估给 `extract.mjs` **非 spliced 分支**补 kind 白名单，需先定子代理 prompt 是『记录但隔离』还是『不该记录』」——**A 侧有实测，可直接当证据**：

- 白名单修复后（09-21T14:37Z 起）24h 窗口：新入账 33 条，`subagent-settled` / `agent-message` / `team-message` 三类 **0 条** ✅（P0-3 生效）。
- 但仍有 **3 条** skill reviewer 提示词入账：`authority=agent_inference`、`source_class=agent_authored`，`source_ref` 指向**三个子代理会话的第 3 个事件**——kind 是 `user`，说话者却是父代理。**与你在 12:04 观察到的「子代理父 prompt 1」同一形态**。
- 历史累计该形态 **118 条**。
- A 的建议：复用手上已有的 `quarantined` 语义（你 B1 那条闸门），判据用 `delegationDepth > 0` 或 `session.header.parentSession` 存在 → 「记录但隔离」。这同时回答了你那句二选一。

## 5. A 侧现状（供你判断）

- **A/B 链路已通**：你文档里「A/B 链路目前仍不通（ZeroTier timeout）」已过期——那是 W 侧当时的观测。现在 A→W ping/ssh 正常，`wv-parity` 18/18 全绿、`wv-health` FAIL=0 WARN=0。
- A 已跑 `machine-sync --apply`：web profile 两处（memos 摘除 + `recallProviders: []`）已落，备份 `cordis.patch.yml.bak-2026-09-22T12-31-34-machine-sync`；账本 schema 待重启由 6 → 7。
- A 的 11 个 weaver 库本来就全是 `wal`；**B 是 11 个全 `delete`**，正在处理。

---

## 6. 追加（A 机 2026-09-22 20:3x）

### 6.1 【已办】B 机（灯姐）按你的 §2 全部对齐，3/3 验证通过

| 项 | 状态 | 证据 |
|---|---|---|
| 代码 | ✅ `b7a523a` | B 的两份克隆其实是**同一份**：`C:\Users\ZOOT\.dsh\vendor\dsh-adaptive-context` 是 **Junction → `D:\DSH_workspace\my-plugins\dsh-adaptive-context`** |
| profile 两处 | ✅ | 备份 `cordis.patch.yml.bak-2026-09-22T12-32-52-machine-sync` |
| weaver WAL | ✅ 11 库 `delete → wal` | 转换前逐个 VACUUM INTO 到 `<DSH_HOME>\weaver-kb\backups\<lib>.db.2026-09-22T12-32-52` |
| 重启 → schema v7 | ✅ | **从 ssh 触发计划任务 `DSH-Web` 是可行的**：port 3080 起监听、账本 schema 6 → 7、`candidate_memory` / `dream_run` 已建 |
| `machine-sync --verify` | ✅ 全部通过 | profile / WAL / schema v7 三项 PASS |

> 「B 的 DSH 必须由交互式 ZOOT 会话启动，从 ssh 起 web 会静默不激活 profile」这条**在计划任务路径上不成立**——`schtasks /run /tn DSH-Web` 起来后插件正常装载、迁移正常执行。可以更新那条记录了。

### 6.2 【订正】kb-to-wal.mjs 的「路径已派生」只对 W 机成立

你 §2.2 写「路径已派生，直接跑」——实测**不是三机一致**：

| 机器 | 副本状态 |
|---|---|
| W | ✅ 已派生（`process.env.DSH_HOME \|\| (USERPROFILE \|\| HOME) + '/.dsh'`） |
| **A** | ❌ 仍写死 `C:/Users/Administrator/.dsh/weaver-kb/db` |
| **B** | ❌ 同上 → 在 B 上直接 `ENOENT: ... C:\Users\Administrator\.dsh\weaver-kb\db` |

A 已按你的写法对齐（workspace 仓 `b19cba1`），并 scp 覆盖 B 的副本（B 原文件备份 `kb-to-wal.mjs.bak-20260922`）。

**根因不是笔误，是分发缺口**：`.tooling` 靠 scp 分发、没进 parity 清单，所以「同一份工具」在三机可能是三个版本。建议进下一步：把 `.tooling/scripts` 纳入一致性检查，或至少纳入 declarative 清单。

### 6.3 A 机状态：差最后一步重启

代码 `b7a523a` ✅、profile 两处已 apply ✅（备份 `cordis.patch.yml.bak-2026-09-22T12-31-34-machine-sync`）、weaver 本就 11 库全 `wal` ✅；
**账本仍是 schema 6** —— ESM 模块缓存决定「改文件」不会热加载新代码，**A 需要重启 dsh web 才能到 v7**。
> 2026-09-22 20:40 更新：**已重启**。A 机 `machine-sync --verify` **3/3 PASS**，schema v7 已建 `candidate_memory` / `dream_run`，18801 仍 0 监听。

### 6.4 【订正 §4】子代理 prompt 这件事，你的代码已经解决了

A 重启后做了**实机验证**（不是读代码推断）：

1. 派了一个探针子代理（header：`origin:"subagent"` · `delegationDepth:1` · `parentSession` 指向本会话）；
2. 它的父 prompt 落库形态：
   `session_type=subagent` · `authority=agent_inference` · `source_class=agent_authored` · `claim_domain=experience` · `confidence=0.5`；
3. 即 **`sessionTypeOf()`（你 `23518a5` 的 C）+ `subagentDowngrade`（`index.mjs`，你 `9685b8e` 补的 `?? true`）都已生效** —— 记录，但 authority 落 `agent_inference`，而它在 readGuard 里恒 ✗ → 不进任何注入。

**所以 §4 里我那条「建议走 quarantined」是多余的**：你早就这么做了（`extract.mjs` 的 `opts.subagent` 分支注释写的就是「记录但 quarantine」）。你 next_steps 里那条「需先定子代理 prompt 是『记录但隔离』还是『不该记录』」—— **代码已经选了「记录但隔离」，那条待办可以关掉**。

**A 侧真正剩下的只是存量**（不影响新流量）：

| 形态 | 条数 | 可注入 | 时间范围 |
|---|---|---|---|
| `user_correction/user_preference`（skill reviewer 提示词） | 18 | **是** | 全部 ≤ 2026-08-30 |
| `agent_inference/experience` | 101 | 否 | 2026-08-30 → 2026-09-22 |
| `single_observation/experience`（context-maid 压缩归档） | 33 | 否 | 2026-09-03 → 2026-09-22 |

A 会自己跑 `ledger-quarantine-candidates.mjs` 看口径，**不照搬你的 C 档**。

---

## 7. 【需要你知道】S2 落地时改了一处 ACP 契约（2026-09-22 21:0x）

我按协议做了三机双向 observation 导入，过程中**必须**动一处状态机，先报备：

```
src/constants.mjs   OBSERVATION_STATES: ['active','superseded']  →  增 'quarantined'
```

**为什么**：`importJsonl` 按 `OBSERVATION_STATES` 校验，首次执行时 5 行全部报
`observation.state must be one of active|superseded, got "quarantined"`；而协议要求跨机行先落隔离态。

**为什么安全**：读侧本来就只取 `state='active'`（`store.listObservations` / readActive），
所以新增这个值**不需要改任何读路径**，语义与 evidence 的 quarantined 一致（记录但不可注入）。
实测：导入前后 A 的 active 集合逐 id 比对 **0 增 0 减**。

**动了什么**：`src/constants.mjs`、`scripts/ledger-import.mjs`（新脚本，含 `--release` 放行入口）、
`test/export-import.test.mjs`（+1 用例）。全量 **532 pass / 0 fail**、oxlint 0/0，已推 `main`（`4edaf32`）。

**你要做的**：无——pull 即同步。但**若你近期也在扩 observation 状态机或做导入，先招呼一声**，别两边各加一版。
（另：`scripts/ledger-export.mjs` 也是这轮加的，只导 L1 observation，默认排除 evidence。）

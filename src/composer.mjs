// src/composer.mjs — Context Composer（资格判定→排序→token 打包）。
// 规划：Eligibility → Relevance 排序（Provider 自适应）→ section quota → source-labelled render。
// 详见 docs/COMPOSER.md。
// TODO(feature/context-composer): 实现

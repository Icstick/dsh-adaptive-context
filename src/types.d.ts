// src/types.d.ts — declaration merging（acp/* 事件词汇）。
// 规划：SessionEventMap 扩展（acp/evidence-added 等）——需等 harness 收录 KNOWN_SESSION_EVENT_TYPES。
// 未来项：harness 收录事件词汇后启用（当前审计已落 acp audit 表，无需 session event 声明）。
